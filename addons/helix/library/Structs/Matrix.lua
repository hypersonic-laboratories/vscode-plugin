-- Matrix struct
-- A Matrix is an array of numbers this can be used for geometric and positional calculations. This is mainly used internally.
Matrix = {}

-- @param rotation (Rotator) The rotation of the matrix
-- @param origin (Vector) The origin of the matrix
-- Default Constructor
function Matrix.new(rotation, origin) end


